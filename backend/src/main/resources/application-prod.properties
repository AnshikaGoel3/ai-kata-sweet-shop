# --- Final Robust Configuration ---

# Server Port: Use the Railway PORT variable
server.port=${PORT:8080}

# Database Connection: Using the Public URL and explicit credentials to bypass Railway's unstable internal links.
# Credentials and Public Host/Port are taken from your environment variables/connect screen.

# 1. Database URL (Host/Port/Database Name)
spring.datasource.url=jdbc:postgresql://maglev.proxy.rlwy.net:47984/railway

# 2. Database Username
spring.datasource.username=postgres

# 3. Database Password
spring.datasource.password=cIplTDDLSRrZmscPIlequNJdHMgENNAi

# Driver and JPA Settings (Kept as before)
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# --- End of Configuration ---